# CLAUDE.md

This repository is **grok-senpai**. The canonical playbook is `AGENTS.md`.

Claude Code may be:

- the **senpai host** (experimental) via `.claude/skills/senpai/`
- a **worker** via `.grok/skills/claude-worker/`

Launch workers only through `.grok/orchestration/senpai.sh`. Never `eval`.
